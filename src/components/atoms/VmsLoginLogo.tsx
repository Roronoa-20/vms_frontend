import React from "react";
import Image from "next/image";
const VMSLogo = () => {
  return (
    <Image
      src={"/vms_logo.png"}
      alt=""
      width={300}
      height={100}
    />
  );
};

export default VMSLogo;
